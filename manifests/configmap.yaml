apiVersion: v1
kind: ConfigMap
metadata:
  name: taskapp-config
  namespace: taskapp
data:
  DATABASE_URL: "postgresql://postgres:$(DB_PASSWORD)@postgres:5432/taskapp"
  FLASK_ENV: "production"
  FLASK_APP: "app.py"
  # Frontend API URL
  REACT_APP_API_URL: "https://api.taskapp.sopuruchi.duckdns.org"